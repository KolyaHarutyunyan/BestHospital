import { makeStyles } from "@material-ui/core/styles";

export const branchesStyle = makeStyles(() => ({
  branchesStyle: {
    display: "flex",
    justifyContent: "space-between",
    alignItems: "center",
  },
}));
