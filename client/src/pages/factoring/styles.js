import { makeStyles } from "@material-ui/core/styles";

export const factoringStyle = makeStyles(() => ({
  factoringStyle: {
    display: "flex",
    justifyContent: "space-between",
    alignItems: "center",
  },
}));
