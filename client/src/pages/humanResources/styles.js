import { makeStyles } from "@material-ui/core/styles";

export const humanResourcesStyle = makeStyles(() => ({
  humanResourcesStyle: {
    display: "flex",
    justifyContent: "space-between",
    alignItems: "center",
  },
}));
