import { makeStyles } from "@material-ui/styles";

export const fundingSourceCommonStyle = makeStyles(() => ({
   modifierTableStyle: {
      width: "100%",
      minWidth: "729px",
      overflowX: "auto",
      marginTop: "24px",
      paddingBottom: "24px",
   },
}));
