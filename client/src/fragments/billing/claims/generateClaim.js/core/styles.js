import { makeStyles } from "@material-ui/core/styles";

export const generateClaimCoreStyle = makeStyles(() => ({
   incompleteBillTableStyle: {
      width: "100%",
      marginBottom: "20px",
   },
}));
