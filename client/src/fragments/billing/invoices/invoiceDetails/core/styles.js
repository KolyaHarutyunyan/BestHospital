import { makeStyles } from "@material-ui/core/styles";

export const invoiceDetailsCoreStyle = makeStyles(() => ({
   receivableContainerStyle: {
      width: "100%",
      marginTop: "16px",
   },
}));
