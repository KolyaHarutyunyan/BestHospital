import React from "react";
import { Card } from "components";

export const HelpCenterFragment = () => {
   return (
      <div className="help-center-fragment">
         <Card cardBackgroundColor={""}>help center fragment</Card>
      </div>
   );
};
