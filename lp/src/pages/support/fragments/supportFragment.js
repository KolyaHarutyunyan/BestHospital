import React from "react";
import { Card } from "components";

export const SupportFragment = () => {
  return (
    <div className="support-fragment">
        <Card cardBackgroundColor={""}>
            support fragment
        </Card>
    </div>
  );
};
