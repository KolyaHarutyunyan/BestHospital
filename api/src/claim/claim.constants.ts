export enum ClaimStatus {
    PENDING = "PENDING",
    CLOSED = "CLOSED",
    SUBMITTED = "SUBMITTED",
    POSTED = "POSTED"
}
export enum MergeClaims {
    OFF = "OFF",
    ON = "ON"
}

export enum ReceivableStatus {
    CLOSE = "CLOSE",
}